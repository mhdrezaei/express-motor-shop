<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <title>Motor Shop</title>
</head>
<body>
    <h1 class="main-title">Wellcome to motor shop</h1>
    <main>
        <div class="container">
            <div class="row">
                <% for(motor of motors){ %>
                <div class="product">
                    <div class="product__content">
                        <div class="product__discount">%<%=motor.discount%> off</div>
                        <div class="product__img">
                            <img src="/images/<%=motor.image%>" alt="<%=motor.name%>">
                        </div>
                        <a href="/product?id=<%=motor.id%>">
                        <h3 class="product__title"><%=motor.name%></h3>
                    </a>
                        <p class="product__rating" ></p>
                        <p class="product__price"> $<% if(motor.hasDiscount){ %>
                            <%=motor.orginalPrice * (100 - motor.discount) /100 %>
                            <% } else {%>
                                <%=motor.orginalPrice%>
                                <%}%> &nbsp; <span class="product__price--discount">$<%=motor.orginalPrice%></span></p>
                    </div>
                </div>

                <% } %>
            </div>
        </div>
    </main>
</body>
</html>